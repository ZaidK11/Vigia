# Memory — 2026-05-19

## Session: Vigia Portal — Compliance Dashboard Build & GitHub Push

### GitHub Repository
- Repo: `ZaidK11/Vigia` (github.com/ZaidK11/Vigia)
- Branch: `main`
- PAT: stored in 1Password vault `sophia-agent-zaid` → item `GitHub Personal Access Token`
  - Auth as: `ZaidK11`
  - Required scope: `Contents: Read and write` (fine-grained PAT)
- Secret ref: `op://sophia-agent-zaid/GitHub Personal Access Token/token`
- TOOLS.md updated with GitHub ref

### What Was Built & Pushed (commit 515d0cd)
1. `server/jobs/updateDashboard.js` — cron job (node-schedule, :00 and :30 every hour)
   - Pulls: Freshdesk open tickets, Jira KS/AR/COM projects, Elliptic sanctions from ClickHouse
   - Merges, calculates due dates + days left, sorts by urgency
   - In-memory cache with 35-min TTL
2. `server/routes/dashboard.js` — `GET /api/dashboard` + `POST /api/dashboard/refresh`
3. `client/src/pages/Dashboard.jsx` — full React UI
   - Stats cards (Open Cases, Due Today, Overdue, Sources)
   - Filter tabs (All, Support, KYC, TM, Fraud, Escalations, Overdue, Due Today)
   - Sortable queue table with color-coded urgency rows
   - Click-to-detail panel with case info + action buttons
   - Auto-refresh every 30s
4. `DEPLOYMENT.md` — full deployment guide
5. `Dockerfile` updated to Node 18-alpine, PORT=3000

### Jira Fix (important)
- During build, discovered Jira 410 error with `project = KYC` — correct project key is `KS` (not `KYC`)
- Fixed in updateDashboard.js
- Jira base URL: `https://airtech.atlassian.net` (not airtm.atlassian.net)

### Full Test Results (all pass)
| User | Role | Result |
|---|---|---|
| zaid.khan@airtm.io | LEADERSHIP | ✅ All 6 portals |
| melanie.suckau@airtm.io | TM_ANALYST | ✅ TM + Support |
| omar.isea@airtm.io | FRAUD_INVESTIGATOR | ✅ Fraud + Support |
| jose.crespo@airtm.io | KYC_ANALYST | ✅ KYC + Support |
| alexis.vicente@airtm.io | SUPPORT_ANALYST | ✅ Support only |
| random@gmail.com | — | ✅ Blocked |
- Dashboard: 250 real cases loaded, stats, filters, click-detail all working
- Role enforcement: ✅ API + UI both enforce role restrictions
- Google SSO: `hd: 'airtm.io'` domain lock in place, awaiting GOOGLE_CLIENT_ID/SECRET

### Railway Deployment — PENDING (as of 3:15 AM CST)
- CKE cannot complete Railway deploy — requires Zaid's GitHub OAuth session on railway.app
- All env vars prepared and shared with Zaid for paste into Railway Variables tab
- Zaid's next steps:
  1. railway.app → New Project → GitHub → ZaidK11/Vigia → main
  2. Paste env var block (shared in Slack) into Variables → Raw Editor
  3. Share Railway URL for final test

### Env Vars for Railway (already shared with Zaid in Slack)
All secrets confirmed present in 1Password vault `sophia-agent-zaid`:
- JIRA_TOKEN ✅
- FRESHDESK_KEY ✅
- CLICKHOUSE_PASSWORD ✅
- ANTHROPIC_API_KEY ✅
- PERSONA_API_KEY ✅
- ELLIPTIC_API_KEY ✅
- SESSION_SECRET (generated) ✅
- Google OAuth: NOT YET — needs Google Cloud Console project (can be done post-launch)

### Portal Architecture (confirmed working)
- Server: Node 18 + Express, port 3000
- Client: React + Vite + Tailwind, pre-built into server/public/
- Auth: email whitelist (server/data/users.json) + bcrypt passwords + session cookies
- 6 portals: Support, Fraud, KYC, TM, Leadership, Dashboard
- Dashboard cron auto-starts on server startup

### Open Items
- [ ] Zaid to complete Railway connection + paste env vars
- [ ] Zaid to share Railway URL → CKE will run final production test
- [ ] Google OAuth setup (post-launch, optional for now)
- [ ] Update DEPLOYMENT.md with actual Railway URL once live

---

## Session: Production Docs & Launch Prep (12:20 CST)

Zaid confirmed portal is production-ready and assigned ownership to CKE.

### Deliverables created and pushed to ZaidK11/Vigia (commit ecd628f):

1. `VIGIA_PRODUCTION_RUNBOOK.md` — full ops guide (architecture, env vars, health check, login/roles, cron, audit trail, deployment, troubleshooting, known limitations, escalation contacts)
2. `SUPPORT_TEAM_TRAINING.md` — 7-module training guide for support agents (workflow, risk levels, alert banners, common scenarios)
3. `VIGIA_REFERENCE_CARD.md` — one-page quick reference card for support team
4. `VIGIA_MONITORING_SETUP.md` — monitoring setup (UptimeRobot, CKE heartbeat, error tracking, performance baselines, incident response)
5. `JUNE1_LAUNCH_CHECKLIST.md` — week-by-week checklist for June 1 launch

### Three critical blockers before June 1 (flagged to Zaid):
1. API-level role enforcement (currently frontend-only — security risk)
2. FRESHDESK_KEY truncation in Railway (Dashboard shows 0 Freshdesk tickets)
3. Persistent audit trail (Railway volume or external DB — compliance requirement)

### Status
- All 5 docs on GitHub ✅
- Railway NOT re-deployed (docs don't trigger Railway deploy — no server/client changes)
- Portal still live at https://vigia-production-5a0a.up.railway.app

---

## Support Portal v2 Enhancement (12:37 CST)

Zaid shared two spec files: SUPPORT_PORTAL_ENHANCEMENT_SPEC.md + VIGIA_COMMAND_FIX_SUPPORT_PORTAL.md

### Built and pushed (commit 15519e2):

**Backend (server/routes/support.js):**
- `computeRiskScore(user, ticket, priorTickets)` — composite risk scoring:
  - Account age (<7d → +25, <30d → +10, 1yr+ → -10)
  - KYC document verification (unverified → +20, verified → -5)
  - Internal risk level (HIGH +30, MEDIUM +15, LOW -5)
  - Watchlist (not cleared → +15, clear → -5)
  - Prior contact frequency (>5 tickets → +10)
  - Ticket priority (Urgent → +10)
  - TODO comment for Kount when API key available
  - Returns: score, level (CRITICAL/HIGH/MEDIUM/LOW), verdict (ESCALATE/VERIFY/APPROVE), confidence, factors, nextSteps
- `buildTicketSummary(ticket, user, conv)` — plain-English summary from first message + account context
- Fixed ticket queue: removed stale `new_and_my_open` filter, added hoursOpen/isUrgent/urgencyScore, sort by urgencyScore DESC
- Added `risk` and `summary` fields to `/ticket/:id` response

**Frontend (client/src/pages/Support.jsx):**
- Auto-refresh queue every 30s + manual ↻ button + "refreshed X ago" display
- Vigía Verdict card: risk factors with ❌/✅/⚠️ icons and score weights, next steps, quick action button
- Issue Summary card (blue) before conversation
- Orange highlighting for urgent tickets (⚠️ prefix + orange timer)
- Escalate button in verdict card for ESCALATE verdict (not just at bottom)
- Rebuilt and deployed to server/public/

---

## Master Build — RBAC + Portal Redesigns (14:07 CST)

Zaid sent comprehensive spec. Built and pushed commit e832a41.

### Changes:
**RBAC (frontend + backend):**
- Support portal routes: SUPPORT_ANALYST only (ticket ops)
- /api/support/search: open to all roles (shared by Fraud portal)
- Fraud: FRAUD_INVESTIGATOR + LEADERSHIP
- KYC: KYC_ANALYST + LEADERSHIP
- TM: TM_ANALYST + LEADERSHIP
- Dashboard: SUPPORT_ANALYST + LEADERSHIP
- Audit: LEADERSHIP only
- Backend requireRole() now wired to all routes

**Design:**
- New logo: blue square with dual-V SVG mark (replacing green V)
- Nav: role-filtered tabs, blue active state
- PortalSelector: icon cards with per-portal accent colors (blue/red/purple/amber/green)

**Support portal:** Search User PRIMARY, queue is collapsible secondary card. Ticket detail order: summary → prior history → account context → Vigía verdict → conversation → reply.

**Fraud portal:** Search PRIMARY, cases grouped by status (escalate/active/low), case detail with "tools to check" guidance.

**KYC portal:** Search PRIMARY, status cards (lazy-load list), 15-row limit (was 50) for performance.

**TM portal:** Search PRIMARY, alerts grouped by status with collapsible lists, SAR countdown prominent (color-coded by urgency).

### Clarification raised by spec (MEDIUM priority):
- Part 8: Leadership Overview vs Dashboard — are they the same? Spec asks to consolidate. Left both for now pending Zaid direction.
# Memory — 2026-05-19

## Session: Vigia Portal — Compliance Dashboard Build & GitHub Push

### GitHub Repository
- Repo: `ZaidK11/Vigia` (github.com/ZaidK11/Vigia)
- Branch: `main`
- PAT: stored in 1Password vault `sophia-agent-zaid` → item `GitHub Personal Access Token`
  - Auth as: `ZaidK11`
  - Required scope: `Contents: Read and write` (fine-grained PAT)
- Secret ref: `op://sophia-agent-zaid/GitHub Personal Access Token/token`
- TOOLS.md updated with GitHub ref

### What Was Built & Pushed (commit 515d0cd)
1. `server/jobs/updateDashboard.js` — cron job (node-schedule, :00 and :30 every hour)
   - Pulls: Freshdesk open tickets, Jira KS/AR/COM projects, Elliptic sanctions from ClickHouse
   - Merges, calculates due dates + days left, sorts by urgency
   - In-memory cache with 35-min TTL
2. `server/routes/dashboard.js` — `GET /api/dashboard` + `POST /api/dashboard/refresh`
3. `client/src/pages/Dashboard.jsx` — full React UI
   - Stats cards (Open Cases, Due Today, Overdue, Sources)
   - Filter tabs (All, Support, KYC, TM, Fraud, Escalations, Overdue, Due Today)
   - Sortable queue table with color-coded urgency rows
   - Click-to-detail panel with case info + action buttons
   - Auto-refresh every 30s
4. `DEPLOYMENT.md` — full deployment guide
5. `Dockerfile` updated to Node 18-alpine, PORT=3000

### Jira Fix (important)
- During build, discovered Jira 410 error with `project = KYC` — correct project key is `KS` (not `KYC`)
- Fixed in updateDashboard.js
- Jira base URL: `https://airtech.atlassian.net` (not airtm.atlassian.net)

### Full Test Results (all pass)
| User | Role | Result |
|---|---|---|
| zaid.khan@airtm.io | LEADERSHIP | ✅ All 6 portals |
| melanie.suckau@airtm.io | TM_ANALYST | ✅ TM + Support |
| omar.isea@airtm.io | FRAUD_INVESTIGATOR | ✅ Fraud + Support |
| jose.crespo@airtm.io | KYC_ANALYST | ✅ KYC + Support |
| alexis.vicente@airtm.io | SUPPORT_ANALYST | ✅ Support only |
| random@gmail.com | — | ✅ Blocked |
- Dashboard: 250 real cases loaded, stats, filters, click-detail all working
- Role enforcement: ✅ API + UI both enforce role restrictions
- Google SSO: `hd: 'airtm.io'` domain lock in place, awaiting GOOGLE_CLIENT_ID/SECRET

### Railway Deployment — PENDING (as of 3:15 AM CST)
- CKE cannot complete Railway deploy — requires Zaid's GitHub OAuth session on railway.app
- All env vars prepared and shared with Zaid for paste into Railway Variables tab
- Zaid's next steps:
  1. railway.app → New Project → GitHub → ZaidK11/Vigia → main
  2. Paste env var block (shared in Slack) into Variables → Raw Editor
  3. Share Railway URL for final test

### Env Vars for Railway (already shared with Zaid in Slack)
All secrets confirmed present in 1Password vault `sophia-agent-zaid`:
- JIRA_TOKEN ✅
- FRESHDESK_KEY ✅
- CLICKHOUSE_PASSWORD ✅
- ANTHROPIC_API_KEY ✅
- PERSONA_API_KEY ✅
- ELLIPTIC_API_KEY ✅
- SESSION_SECRET (generated) ✅
- Google OAuth: NOT YET — needs Google Cloud Console project (can be done post-launch)

### Portal Architecture (confirmed working)
- Server: Node 18 + Express, port 3000
- Client: React + Vite + Tailwind, pre-built into server/public/
- Auth: email whitelist (server/data/users.json) + bcrypt passwords + session cookies
- 6 portals: Support, Fraud, KYC, TM, Leadership, Dashboard
- Dashboard cron auto-starts on server startup

### Open Items
- [ ] Zaid to complete Railway connection + paste env vars
- [ ] Zaid to share Railway URL → CKE will run final production test
- [ ] Google OAuth setup (post-launch, optional for now)
- [ ] Update DEPLOYMENT.md with actual Railway URL once live

---

## Session: Production Docs & Launch Prep (12:20 CST)

Zaid confirmed portal is production-ready and assigned ownership to CKE.

### Deliverables created and pushed to ZaidK11/Vigia (commit ecd628f):

1. `VIGIA_PRODUCTION_RUNBOOK.md` — full ops guide (architecture, env vars, health check, login/roles, cron, audit trail, deployment, troubleshooting, known limitations, escalation contacts)
2. `SUPPORT_TEAM_TRAINING.md` — 7-module training guide for support agents (workflow, risk levels, alert banners, common scenarios)
3. `VIGIA_REFERENCE_CARD.md` — one-page quick reference card for support team
4. `VIGIA_MONITORING_SETUP.md` — monitoring setup (UptimeRobot, CKE heartbeat, error tracking, performance baselines, incident response)
5. `JUNE1_LAUNCH_CHECKLIST.md` — week-by-week checklist for June 1 launch

### Three critical blockers before June 1 (flagged to Zaid):
1. API-level role enforcement (currently frontend-only — security risk)
2. FRESHDESK_KEY truncation in Railway (Dashboard shows 0 Freshdesk tickets)
3. Persistent audit trail (Railway volume or external DB — compliance requirement)

### Status
- All 5 docs on GitHub ✅
- Railway NOT re-deployed (docs don't trigger Railway deploy — no server/client changes)
- Portal still live at https://vigia-production-5a0a.up.railway.app

---

## Support Portal v2 Enhancement (12:37 CST)

Zaid shared two spec files: SUPPORT_PORTAL_ENHANCEMENT_SPEC.md + VIGIA_COMMAND_FIX_SUPPORT_PORTAL.md

### Built and pushed (commit 15519e2):

**Backend (server/routes/support.js):**
- `computeRiskScore(user, ticket, priorTickets)` — composite risk scoring:
  - Account age (<7d → +25, <30d → +10, 1yr+ → -10)
  - KYC document verification (unverified → +20, verified → -5)
  - Internal risk level (HIGH +30, MEDIUM +15, LOW -5)
  - Watchlist (not cleared → +15, clear → -5)
  - Prior contact frequency (>5 tickets → +10)
  - Ticket priority (Urgent → +10)
  - TODO comment for Kount when API key available
  - Returns: score, level (CRITICAL/HIGH/MEDIUM/LOW), verdict (ESCALATE/VERIFY/APPROVE), confidence, factors, nextSteps
- `buildTicketSummary(ticket, user, conv)` — plain-English summary from first message + account context
- Fixed ticket queue: removed stale `new_and_my_open` filter, added hoursOpen/isUrgent/urgencyScore, sort by urgencyScore DESC
- Added `risk` and `summary` fields to `/ticket/:id` response

**Frontend (client/src/pages/Support.jsx):**
- Auto-refresh queue every 30s + manual ↻ button + "refreshed X ago" display
- Vigía Verdict card: risk factors with ❌/✅/⚠️ icons and score weights, next steps, quick action button
- Issue Summary card (blue) before conversation
- Orange highlighting for urgent tickets (⚠️ prefix + orange timer)
- Escalate button in verdict card for ESCALATE verdict (not just at bottom)
- Rebuilt and deployed to server/public/

---

## Master Build — RBAC + Portal Redesigns (14:07 CST)

Zaid sent comprehensive spec (VIGIA_MASTER_BUILD_COMMAND.md). Built and pushed commit e832a41.

### Role mapping (final, as of this build):
| Role | Portals |
|---|---|
| SUPPORT_ANALYST | Support + Dashboard |
| FRAUD_INVESTIGATOR | Fraud + Dashboard |
| KYC_ANALYST | KYC + Dashboard |
| TM_ANALYST | TM + Dashboard |
| LEADERSHIP | All 6 |

- Support sees: Support + Dashboard only (no compliance portals)
- Compliance sees: their portal + Dashboard (no Support portal)
- /api/support/search stays open to all roles (Fraud portal uses it for account lookup)
- Backend requireRole() now wired to all routes (was frontend-only before)

### Design changes:
- New logo: blue `#0066FF` square with dual-V SVG mark
- Nav: role-filtered tabs, blue active underline
- PortalSelector: accent cards per portal (blue/red/purple/amber/green/slate)

### Portal-by-portal:
**Support:** Search is now PRIMARY (top, always visible). Queue is collapsible secondary card. Ticket detail order: summary → prior history → account context → Vigía verdict → conversation → reply template.

**Fraud:** Search PRIMARY. Cases in 3 collapsible status groups: 🔴 Ready to Escalate / 🟡 Under Investigation / 🟢 Low Risk. Case detail has "tools to check" guidance panel (Elliptic, Kount, Jira AR links).

**KYC:** Search PRIMARY. Applications as status count cards (lazy-load), list is collapsible. Initial page load is lightweight — no more 500-row query at load time.

**TM:** Search PRIMARY. Alerts in 3 collapsible status groups. SAR countdown shown prominently with color-coding (red ≤5d, orange ≤15d). 3-hour SLA banner on open alerts.

### Open question raised to Zaid (MEDIUM priority):
- Leadership Overview vs Dashboard — spec asked to consider consolidating. Left both pending Zaid direction.

### Vigia portal files (all in vigia-portal/ subdirectory):
- Has its own `.git` repo — push directly from `vigia-portal/` dir, not workspace root
- `cd vigia-portal && git push origin main` is the correct push command
- Railway watches this repo (ZaidK11/Vigia) and auto-deploys on main push

### Production URL
https://vigia-production-5a0a.up.railway.app

### Known remaining issues (post-launch sprint):
1. Google OAuth not configured (email login works — non-blocking)
2. KYC country shows "Unknown" in list view (detail view works — cosmetic)
3. Kount integration stubbed with TODO (needs dot-separated key from Zaid, Merchant 171489)
4. Audit trail not persisted across Railway deploys (needs Railway volume)

---

## Google OAuth Live (23:26 CST)

Zaid set GOOGLE_CLIENT_ID + GOOGLE_CLIENT_SECRET in Railway Variables and redeployed.

### Fixes applied:
- passport.js: removed `proxy:true` (was causing Railway to override callback URL with http://)
- passport.js: hardcoded `https://vigia-production-5a0a.up.railway.app/auth/google/callback`
- index.js: added `app.set('trust proxy', 1)` for Railway SSL
- sso.js: added lazy reinitGoogle() call on each request
- Added `/api/health/sso` diagnostic endpoint

### Final state (commit f5bb083):
- `/api/health/sso` → `{"sso_ready":true,"google_client_id_set":true,...}`
- `/auth/google` → 302 to `accounts.google.com` with correct https callback + hd=airtm.io domain lock
- Google OAuth flow: complete and functional

### Credentials stored:
- 1Password: `vigia-google-oauth` (ID: un32y4if2w2plkufllcy5odqpu) in sophia-agent-zaid
- Client ID: `639610949584-vn628cipn2rtp8dhcn6neg0k1imhnt9q.apps.googleusercontent.com`
- Project: `vigia-495502`
- Callback: `https://vigia-production-5a0a.up.railway.app/auth/google/callback`

### Employee whitelist updated (commit c52ac62):
- 135 employees from AIRTM_EMPLOYEE_MASTER_FILE.csv
- Notable email changes from old list: mishell@, lauras@, eduardon@, franciscos@ etc.
- All roles correctly mapped
