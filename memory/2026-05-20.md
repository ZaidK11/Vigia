
## Vigia Portal Feedback from Zaid (18:32 CST)

Zaid tested the portal and gave positive feedback — fast, responsive, loves the intelligence summaries and recommendations.

### Action Items:
1. **Transactions fix** — Portal is reading from a table with no real tx data. Switch to `analytics_finance.fact_trx` in ClickHouse (table: `fact_trx`) for accurate summaries and verdicts.
2. **Freshdesk filter** — Currently pulling all support tickets. Filter by compliance teams:
   - `1st level - Compliance`
   - `Compliance ARG`, `Compliance IND`, `Compliance KYC`, `Compliance KYB`, `Compliance TM`
   - `Fraud & Risk Prevention`
   - `Verifications - Personal`
3. **Fraud module cleanup** — Remove the "Cases" section under Fraud module (TM alerts already live in the TM Alerts module — duplication).


## Fixes Implemented (19:xx CST)

### Critical fixes committed (vigia-portal), awaiting push to GitHub:

**Fix #1: Language toggle bug (Sergio's find)**
- Root cause: `switchLang()` was calling `analyze(lang)` = full Claude re-analysis in ES
- Fix: Analysis runs ONCE in English. ES toggle calls new `/api/vigia/translate` endpoint
- `/api/vigia/translate` is translation-only with explicit rules: preserve all numbers, confidence %, verdict labels (kept in English always)
- Files: `vigia-portal/server/routes/vigia-api.js`, `client/src/components/VigiaResponse.jsx`, `client/src/lib/api.js`

**Fix #2: Support group filter (Omar's approach)**
- Backend: added `getComplianceGroupIds()` — resolves 8 compliance Freshdesk group names to IDs (cached 1hr)
- `isComplianceTicket()` checks group_id + fallback compliance tag check
- Each ticket now has `isCompliance: true/false` flag
- Frontend: Group filter tabs (All Teams / Compliance / General Support) with live counts
- Compliance tickets get 🔒 badge in queue list
- Files: `vigia-portal/server/routes/support.js`, `client/src/pages/Support.jsx`

**Fix #3: Fraud Cases section removed**
- Cases queue (StatusGroup with escalate/active/monitoring) removed from Fraud page
- TM Alerts module owns the case queue
- Replaced with simple "Jump to Case" text input for analysts who have a case ID from TM Alerts
- Files: `vigia-portal/client/src/pages/Fraud.jsx`

**Fix #4: Transactions → fact_trx**
- Fraud investigation now queries `analytics_finance.fact_trx` instead of `data_lake.payments_kecleon_operations`
- Column mapping: trx_id→id, user_id→airtm_user_id, amount_usd→amount, trx_type→operation_type
- Files: `server/routes/fraud.js` (also vigia-portal copy needs same change)

**GitHub token expired** — push blocked. Zaid needs to push manually or refresh token.
